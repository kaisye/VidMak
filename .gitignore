# Python (conda env 'vidmak' lives outside repo, in anaconda3/envs — nothing to ignore here)
__pycache__/
*.pyc
.pytest_cache/

# Pipeline output: whole projects/ tree is generated per-topic and regenerable
# (analysis.md, storyboard.json, scenes/, media/, final.mp4) -- keep out of git.
# For a fixed test fixture later, add it deliberately under tests/.
projects/
media/
*.mp4
Ref_Video.mp4

# Node / Tauri (Phase 3)
node_modules/
apps/desktop/dist/
apps/desktop/src-tauri/target/

# Env & secrets
.env
.env.local
*.key

# OS
.DS_Store
Thumbs.db

# Claude Code local session state
.claude/
